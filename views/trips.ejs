<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>旅行一覧 - TripMate</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <%- include('partials/header', { username: username }) %>

    <div class="container">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="fw-bold">マイページ / 旅行一覧</h2>
            <a href="/trip_create" class="btn btn-primary shadow-sm">+ 新しい旅行を作成</a>
        </div>

        <div class="row">
            <% if (trips.length > 0) { %>
                <% trips.forEach(trip => { %>
                    <div class="col-md-4 mb-4">
                        <div class="card h-100 shadow-sm">
                            <div class="card-body">
                                <h5 class="card-title fw-bold text-primary"><%= trip.title %></h5>
                                <p class="card-text text-muted mb-1">
                                    <i class="bi bi-calendar"></i> <%= trip.start_date %> 〜
                                </p>
                                <p class="card-text text-muted">
                                    <i class="bi bi-calendar-check"></i> <%= trip.end_date %>
                                </p>
                                <a href="/trip_detail/<%= trip.id %>" class="btn btn-outline-primary btn-sm w-100">詳細を見る</a>
                            </div>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="col-12 text-center py-5 bg-white rounded shadow-sm">
                    <p class="text-muted mb-4">作成された旅行プランがまだありません。</p>
                    <p>右上のボタンから、最初の旅行を計画しましょう！</p>
                </div>
            <% } %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>