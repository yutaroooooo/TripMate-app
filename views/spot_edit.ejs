<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>スポット編集 - TripMate</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/spot-create.css">
</head>
<body class="bg-light">
    <%- include('partials/header', { username: username }) %>

    <div class="container py-3 py-lg-4">
        <div class="d-flex align-items-center mb-3 mb-lg-4 px-2">
            <a href="/trip_detail/<%= spot.tripId %>" class="btn btn-outline-secondary btn-sm me-3">
                <i class="bi bi-arrow-left"></i>
            </a>
            <h2 class="h5 mb-0 fw-bold">スポットを編集</h2>
        </div>

        <div class="row g-3 align-items-stretch">
            <div class="col-12 col-lg-7">
                <div class="card shadow-sm border-0 h-100 mb-2 mb-lg-0">
                    <div class="card-body p-2 p-lg-3 d-flex flex-column">
                        <div class="mb-2">
                            <div class="input-group input-group-sm">
                                <span class="input-group-text bg-white border-end-0"><i class="bi bi-search"></i></span>
                                <input type="text" id="mapSearchInput" class="form-control border-start-0" placeholder="場所を変更する場合は検索">
                            </div>
                        </div>
                        <div id="mapPreview" class="rounded bg-secondary-subtle flex-grow-1" style="min-height: 350px;">
                            地図を読み込み中...
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-12 col-lg-5">
                <div class="card shadow-sm border-0 h-100">
                    <div class="card-body p-3 p-lg-4">
                        <form action="/spots/<%= spot.id %>/update" method="POST">
                            <div class="mb-3 p-3 bg-white border rounded shadow-sm">
                                <label class="form-label small fw-bold text-primary"><i class="bi bi-calendar-check me-1"></i>日付</label>
                                <input type="date" name="visit_date" class="form-control form-control-sm border-0 p-0" value="<%= spot.visit_date %>" required>
                            </div>
            
                            <div class="row g-2 mb-3">
                                <div class="col-6">
                                    <div class="p-2 bg-white border rounded shadow-sm">
                                        <label class="form-label mini-label fw-bold text-muted mb-1">開始時間</label>
                                        <input type="time" name="start_time" class="form-control form-control-sm border-0 p-0" value="<%= spot.start_time %>">
                                    </div>
                                </div>
                                <div class="col-6">
                                    <div class="p-2 bg-white border rounded shadow-sm">
                                        <label class="form-label mini-label fw-bold text-muted mb-1">終了時間</label>
                                        <input type="time" name="end_time" class="form-control form-control-sm border-0 p-0" value="<%= spot.end_time %>">
                                    </div>
                                </div>
                            </div>

                            <div class="mb-3 p-3 bg-light rounded shadow-sm">
                                <label class="form-label small fw-bold text-muted">スポット名</label>
                                <input type="text" name="name" id="spotNameInput" class="form-control border-0 bg-transparent fw-bold p-0" value="<%= spot.name %>" required>
                            </div>

                            <div class="mb-4 p-3 bg-light rounded shadow-sm">
                                <label class="form-label small fw-bold text-muted">住所</label>
                                <input type="text" name="address" id="addressInput" class="form-control border-0 bg-transparent p-0 small" value="<%= spot.address %>">
                            </div>

                            <div class="mb-4">
                                <label class="form-label small fw-bold">Instagram URL</label>
                                <div class="input-group">
                                    <span class="input-group-text bg-white"><i class="bi bi-instagram"></i></span>
                                    <input type="url" name="instagram_url" class="form-control" value="<%= spot.instagram_url %>" placeholder="https://www.instagram.com/p/...">
                                </div>
                            </div>

                            <div class="d-grid gap-2">
                                <button type="submit" class="btn btn-primary btn-lg fw-bold shadow-sm">
                                    変更を保存
                                </button>
                                <a href="/trip_detail/<%= spot.tripId %>" class="btn btn-link text-muted text-decoration-none small">
                                    キャンセル
                                </a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="/js/spot-create.js"></script>
    <script async src="https://maps.googleapis.com/maps/api/js?key=AIzaSyBiDR5uXN-6814XKEM48QIVbyD6PrQn32U&callback=initMap&libraries=places"></script>
</body>
</html>