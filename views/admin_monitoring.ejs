<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>投稿・スポット監視 - TripMate 管理者</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/admin.css">
</head>
<body class="bg-light">
    <%- include('partials/header', { username: user.name }) %>

    <main class="container py-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div class="btn-group shadow-sm">
                <a href="/admin/monitoring?status=all" class="btn btn-outline-dark btn-sm <%= currentStatus === 'all' ? 'active' : '' %>">すべて</a>
                <a href="/admin/monitoring?status=active" class="btn btn-outline-dark btn-sm <%= currentStatus === 'active' ? 'active' : '' %>">有効のみ</a>
                <a href="/admin/monitoring?status=deleted" class="btn btn-outline-dark btn-sm <%= currentStatus === 'deleted' ? 'active' : '' %>">削除済み</a>
            </div>
            <span class="badge bg-danger">管理モード / <%= currentStatus.toUpperCase() %></span>
        </div>

        <div class="row g-3 mb-5">
            <% if (spots && spots.length > 0) { %>
                <% spots.forEach(spot => { %>
                    <div class="col-12">
                        <div class="card shadow-sm border-0 <%= spot.del_flg === 1 ? 'bg-light opacity-75' : '' %>">
                            <div class="card-body d-flex flex-wrap align-items-center">
                                <div class="flex-grow-1 d-flex align-items-center">
                                    <div class="me-3">
                                        <% if (spot.Trip && spot.Trip.Owner) { %>
                                            <img src="<%= spot.Trip.Owner.profile_image || 'https://ui-avatars.com/api/?name=' + encodeURIComponent(spot.Trip.Owner.username) %>" 
                                                 class="rounded-circle border" style="width: 48px; height: 48px; object-fit: cover;">
                                        <% } else { %>
                                            <div class="bg-secondary text-white rounded-circle d-flex align-items-center justify-content-center" style="width: 48px; height: 48px;">
                                                <i class="bi bi-person-fill"></i>
                                            </div>
                                        <% } %>
                                    </div>
                            
                                    <div>
                                        <div class="d-flex align-items-center mb-1">
                                            <% if (spot.del_flg === 1) { %>
                                                <span class="badge bg-secondary me-2">削除済み</span>
                                            <% } else { %>
                                                <span class="badge bg-info me-2">有効</span>
                                            <% } %>
                                            <span class="small text-muted"><%= spot.createdAt.toLocaleString() %></span>
                                        </div>
                                        <p class="mb-0 <%= spot.del_flg === 1 ? 'text-decoration-line-through text-muted' : 'text-dark' %>">
                                            <strong><%= spot.name %></strong> が追加されました
                                        </p>
                                        <div class="small">
                                            <span class="text-muted">投稿者: <%= spot.Trip && spot.Trip.Owner ? spot.Trip.Owner.username : '不明' %></span>
                                            <span class="mx-2 text-muted">|</span>
                                            <a href="/trip_detail/<%= spot.tripId %>" target="_blank" class="text-primary text-decoration-none fw-bold">
                                                <i class="bi bi-box-arrow-up-right me-1"></i>旅行ID: <%= spot.tripId %> を確認
                                            </a>
                                        </div>
                                    </div>
                                </div>
                                
                                <div class="d-flex gap-2">
                                    <% if (spot.del_flg === 1) { %>
                                        <form action="/spots/<%= spot.id %>/restore" method="POST">
                                            <button type="submit" class="btn btn-success btn-sm fw-bold px-3">
                                                <i class="bi bi-arrow-counterclockwise me-1"></i>復元
                                            </button>
                                        </form>
                                    <% } else { %>
                                        <form action="/spots/<%= spot.id %>/delete" method="POST" onsubmit="return confirm('管理権限でこのスポットを削除しますか？');">
                                            <button type="submit" class="btn btn-danger btn-sm fw-bold px-3">
                                                <i class="bi bi-trash me-1"></i>削除
                                            </button>
                                        </form>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <p class="text-center text-muted">現在、監視対象の新しいデータはありません。</p>
            <% } %>
        </div>

        <nav class="mt-5">
            <ul class="pagination justify-content-center">
                <% for(let i = 1; i <= totalPages; i++) { %>
                    <li class="page-item <%= currentPage === i ? 'active' : '' %>">
                        <a class="page-link" href="/admin/monitoring?page=<%= i %>&status=<%= currentStatus %>"><%= i %></a>
                    </li>
                <% } %>
            </ul>
        </nav>

        <div class="text-center border-top pt-4">
            <a href="/admin/users" class="btn btn-dark btn-lg px-5 py-3 rounded-3 shadow">
                ユーザー管理画面へ
            </a>
        </div>
    </main>

    <script src="/js/admin-monitoring.js"></script>
</body>
</html>