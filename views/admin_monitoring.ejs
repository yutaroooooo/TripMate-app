<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>投稿・スポット監視 - TripMate 管理者</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/admin.css">
</head>
<body class="bg-light">
    <%- include('partials/header', { username: user.name }) %>

    <main class="container py-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <span class="badge bg-danger">管理モード</span>
        </div>

        <div class="row g-3 mb-5">
            <% const posts = [
                { id: 101, user: "ユーザーA", content: "不適切な内容の可能性...", date: "2026/01/27 10:00" },
                { id: 102, user: "ユーザーB", content: "画像付きの投稿メモ", date: "2026/01/27 09:45" }
            ]; %>

            <% posts.forEach(post => { %>
                <div class="col-12">
                    <div class="card shadow-sm border-0">
                        <div class="card-body d-flex flex-wrap align-items-center">
                            <div class="flex-grow-1 mb-3 mb-md-0">
                                <div class="d-flex align-items-center mb-1">
                                    <span class="fw-bold me-2"><%= post.user %></span>
                                    <span class="small text-muted"><%= post.date %></span>
                                </div>
                                <p class="mb-0 text-dark"><%= post.content %></p>
                                <a href="/admin/posts/<%= post.id %>" class="small text-primary text-decoration-none">詳細確認</a>
                            </div>

                            <div class="d-flex gap-2">
                                <button class="btn btn-outline-warning btn-sm fw-bold px-3">非表示</button>
                                <button class="btn btn-danger btn-sm fw-bold px-3" onclick="confirmDelete(<%= post.id %>)">削除</button>
                            </div>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>

        <div class="text-center border-top pt-4">
            <a href="/admin/users" class="btn btn-dark btn-lg px-5 py-3 rounded-3 shadow">
                ユーザー管理画面へ
            </a>
        </div>
    </main>

    <script src="/js/admin-monitoring.js"></script>
</body>
</html>