<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>旅のしおり - <%= trip.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/pdf-preview.css">
</head>
<body class="bg-light">

    <div class="container my-5">
        <div class="text-end mb-4 no-print">
            <button onclick="exportToPDF()" class="btn btn-dark shadow-sm px-4 fw-bold rounded-pill">
                <i class="bi bi-printer-fill me-2"></i>PDFとして保存・印刷
            </button>
            <a href="/trip_detail/<%= trip.id %>" class="btn btn-outline-secondary shadow-sm px-4 rounded-pill">戻る</a>
        </div>

        <div class="bookmark-card shadow border-0">
            <div class="text-center mb-5">
                <p class="text-primary fw-bold mb-1 letter-spacing">TRIPMATE ITINERARY</p>
                <h1 class="display-5 fw-bold bookmark-title text-start"><%= trip.title %></h1>
                <p class="h5 text-muted mt-3">
                    <i class="bi bi-calendar3"></i> <%= trip.start_date %> 〜 <%= trip.end_date %>
                </p>
            </div>

            <div class="row g-5">
                <div class="col-md-7">
                    <h3 class="h5 fw-bold mb-4 border-bottom pb-2">SCHEDULE</h3>
                    <div class="timeline-section">
                        <% if (trip.Spots && trip.Spots.length > 0) { %>
                            <% trip.Spots.forEach(spot => { %>
                                <div class="timeline-item">
                                    <div class="timeline-dot"></div>
                                    <div class="d-flex justify-content-between">
                                        <span class="fw-bold text-primary"><%= spot.start_time %></span>
                                        <span class="small text-muted"><%= spot.visit_date %></span>
                                    </div>
                                    <div class="h6 fw-bold mb-1 text-dark"><%= spot.name %></div>
                                    <div class="small text-muted mb-2"><%= spot.address %></div>
                                </div>
                            <% }); %>
                        <% } else { %>
                            <p class="text-muted small">予定が登録されていません</p>
                        <% } %>
                    </div>
                </div>

                <div class="col-md-5">
                    <h3 class="h5 fw-bold mb-4 border-bottom pb-2">MEMO & ITEMS</h3>
                    <div class="p-3 mb-4 bg-light rounded-3">
                        <p class="small fw-bold text-primary mb-2">共通メモ</p>
                        <div class="small" style="white-space: pre-wrap;"><%= trip.memo || "特になし" %></div>
                    </div>
                    
                    <div class="border p-3 rounded-3">
                        <p class="small fw-bold text-secondary mb-2">チェックリスト</p>
                        <ul class="list-unstyled item-check-list mb-0">
                            <% if (trip.items) { %>
                                <% trip.items.split(/\r?\n/).forEach(item => { %>
                                    <% if (item.trim()) { %>
                                        <li><i class="bi bi-square me-2"></i><%= item.trim() %></li>
                                    <% } %>
                                <% }); %>
                            <% } else { %>
                                <li class="text-muted small">項目がありません</li>
                            <% } %>
                        </ul>
                    </div>
                </div>
            </div>

            <div class="text-center mt-5 pt-5 border-top text-muted small">
                Generated by TripMate app - Have a safe and wonderful trip!
            </div>
        </div>
    </div>

    <script src="/js/pdf-preview.js"></script>
</body>
</html>