<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>サインアップ - TripMate</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        @media (min-width: 992px) {
            .equal-height-row {
                display: flex;
                align-items: stretch;
            }
            .main-visual-container {
                display: flex;
            }
            .main-visual {
                width: 100%;
                height: 100%;
                object-fit: cover;
                border-radius: 0.5rem;
            }
        }
    
        @media (max-width: 991px) {
            .main-visual {
                width: 100%;
                height: 250px;
                object-fit: cover;
                border-radius: 0.5rem;
            }
        }
    </style>
</head>
<body class="bg-light">
    <div class="container py-5">
        <div class="row justify-content-center align-items-stretch equal-height-row g-4">
            
            <div class="col-12 col-lg-6 main-visual-container">
                <img src="/images/signup.jpg" alt="メインビジュアル" class="shadow main-visual">
            </div>

            <div class="col-12 col-lg-5">
                <div class="card shadow-sm h-100">
                    <div class="card-body p-4">
                        <h2 class="card-title text-center mb-4">新規会員登録</h2>
                        
                        <form action="/signup" method="POST">
                            <div class="mb-3">
                                <label class="form-label">ユーザー名</label>
                                <input type="text" name="username" class="form-control" maxlength="20" required>
                                <div class="form-text">20文字以内で入力してください</div>
                            </div>

                            <div class="mb-3">
                                <label class="form-label">メールアドレス</label>
                                <input type="email" name="email" class="form-control" maxlength="100" required>
                            </div>

                            <div class="mb-3">
                                <label class="form-label">パスワード</label>
                                <input type="password" name="password" class="form-control" minlength="8" required>
                                <div class="form-text">8文字以上、半角英数字混合</div>
                            </div>

                            <div class="mb-3">
                                <label class="form-label">パスワード（確認用）</label>
                                <input type="password" name="password_conf" class="form-control" required>
                            </div>

                            <button type="submit" class="btn btn-primary w-100 py-2">登録</button>
                        </form>
                    </div>
                </div>
            </div>

        </div>
    </div>
</body>
</html>