<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>パスワード再設定フォーム - TripMate</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-5">
                <div class="card shadow-sm border-0">
                    <div class="card-body p-4">
                        <h2 class="h5 fw-bold mb-4 text-center">新しいパスワードの設定</h2>
                        <form action="/pwd_form" method="POST">
                            <div class="mb-3">
                                <label class="form-label small fw-bold">新しいパスワード</label>
                                <input type="password" name="password" class="form-control" placeholder="8文字以上" required minlength="8">
                            </div>
                            <div class="mb-4">
                                <label class="form-label small fw-bold">新しいパスワード（確認用）</label>
                                <input type="password" name="password_confirm" class="form-control" placeholder="再度入力してください" required>
                            </div>
                            <div class="d-grid">
                                <button type="submit" class="btn btn-primary fw-bold py-2">パスワードを更新する</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>