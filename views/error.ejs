<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>エラー - TripMate</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/error.css">
</head>
<body class="bg-light d-flex flex-column vh-100">
    <%- include('partials/header', { username: user ? user.name : null }) %>

    <div class="container my-auto">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6 text-center">
                <div class="error-card p-5 bg-white shadow-sm rounded-4">
                    <h1 class="display-5 fw-bold text-danger mb-3">
                        <i class="bi bi-exclamation-triangle-fill me-2"></i>
                        <%= errorTitle || '404 Not Found' %>
                    </h1>

                    <div class="error-message-area mb-5">
                        <p class="lead text-muted">
                            <%= errorMessage || 'システムエラーが発生しました。お手数ですがトップからやり直して下さい' %>
                        </p>
                    </div>

                    <div class="d-grid gap-2 col-8 mx-auto">
                        <a href="/trips" class="btn btn-primary btn-lg fw-bold shadow-sm py-3 rounded-pill">
                            <i class="bi bi-house-door-fill me-2"></i>トップページへ戻る
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="py-5"></div>
</body>
</html>