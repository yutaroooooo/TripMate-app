<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>精算管理 - TripMate</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/settlement.css">
</head>
<body class="bg-light">
    <%- include('partials/header', { username: user.name }) %>

    <main class="container-fluid py-4 px-lg-5">

        <h2 class="h4 fw-bold mb-4">精算管理：<%= trip.title %></h2>

        <div class="row g-4">
            <div class="col-lg-4">
                <div class="card shadow-sm border-0 h-100">
                    <div class="card-body">
                        <h3 class="h6 fw-bold mb-3 border-bottom pb-2">新規立替入力</h3>
                        
                        <form action="/trips/<%= trip.id %>/settlement" method="POST">
                            <div class="mb-3">
                                <label class="form-label small fw-bold">支払者</label>
                                <select class="form-select shadow-sm" name="payer_id">
                                    <option value="<%= user.name %>"><%= user.name %> (あなた)</option>
                                    <option value="memberA">メンバーA</option>
                                </select>
                            </div>

                            <div class="mb-3">
                                <label class="form-label small fw-bold">対象者</label>
                                <select class="form-select shadow-sm" name="target_type">
                                    <option value="all">全員均等</option>
                                    <option value="individual">個別指定</option>
                                </select>
                            </div>

                            <div class="mb-3">
                                <label class="form-label small fw-bold">明細・領収書メモ</label>
                                <textarea class="form-control shadow-sm" id="detailInput" name="raw_text" rows="6" placeholder="例：レンタカー代 12000円&#10;または予約完了メールを貼り付けてください"></textarea>
                            </div>

                            <div class="d-grid gap-2">
                                <button type="button" class="btn btn-primary fw-bold py-2 shadow" id="analyzeBtn">
                                    <i class="bi bi-magic me-2"></i>AI解析実行
                                </button>
                                <button type="button" class="btn btn-outline-success fw-bold py-2 shadow-sm" id="remindBtn">
                                    <i class="bi bi-line me-2"></i>精算リマインド
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>

            <div class="col-lg-4">
                <div class="card shadow-sm border-0 h-100">
                    <div class="card-header bg-white py-3 border-bottom">
                        <h3 class="h6 fw-bold mb-0">立替履歴リスト</h3>
                    </div>
                    <div class="card-body p-0 scroll-area">
                        <div class="list-group list-group-flush" id="historyList">
                            <% if (history && history.length > 0) { %>
                                <% history.forEach(item => { %>
                                    <div class="list-group-item p-3">
                                        <div class="d-flex justify-content-between align-items-start mb-1">
                                            <span class="fw-bold"><%= item.title %></span>
                                            <span class="badge bg-secondary rounded-pill">¥<%= item.amount.toLocaleString() %></span>
                                        </div>
                                        <div class="small text-muted mb-2">
                                            支払者: <%= item.payer === user.name ? 'あなた' : item.payer %> | 対象: <%= item.target %>
                                        </div>
                                        <div class="d-flex gap-2">
                                            <button class="btn btn-sm btn-outline-secondary py-0 px-3 rounded-pill">編集</button>
                                            <button class="btn btn-sm btn-outline-danger py-0 px-3 rounded-pill" onclick="confirmDelete(<%= item.id %>)">削除</button>
                                        </div>
                                    </div>
                                <% }) %>
                            <% } else { %>
                                <div class="text-center py-5 text-muted">
                                    <i class="bi bi-receipt d-block mb-2 opacity-25" style="font-size: 3rem;"></i>
                                    <p>まだ立替履歴はありません</p>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-lg-4">
                <div class="card shadow-sm border-0 bg-dark text-white h-100 overflow-hidden">
                    <div style="height: 6px; background: linear-gradient(90deg, #ffc107, #ff8c00);"></div>
                    <div class="card-body text-center d-flex flex-column justify-content-center p-4">
                        <h3 class="h6 fw-bold mb-4 text-uppercase tracking-wider">精算サマリー</h3>
                        
                        <div class="summary-box p-4 border border-secondary rounded-4 mb-4">
                            <p class="small text-secondary mb-3">最終的な精算結果</p>
                            <% if (summary && summary.total > 0) { %>
                                <div class="h5 mb-3 text-warning fw-bold">
                                    <%= summary.from %> <i class="bi bi-arrow-right mx-1"></i> <%= summary.to %>
                                </div>
                                <div class="display-5 fw-bold mb-0 text-white">
                                    ¥<%= summary.total.toLocaleString() %>
                                </div>
                            <% } else { %>
                                <div class="text-muted small">貸し借りは現在ありません</div>
                            <% } %>
                        </div>
                        
                        <div class="mt-2">
                            <i class="bi bi-info-circle me-1"></i>
                            <span style="font-size: 0.75rem;">全ての貸し借りを相殺した金額です</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <script src="/js/settlement.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>