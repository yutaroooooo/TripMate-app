<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>旅行編集 - TripMate</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/trip-edit.css">
</head>
<body class="bg-light">
    <%- include('partials/header', { username: username }) %>

    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-lg-10"> <form action="/trip_edit/<%= trip.id %>" method="POST" enctype="multipart/form-data">
                    <div class="card shadow-sm border-0 p-4">
                        <h2 class="h4 fw-bold mb-4">旅行情報編集</h2>
                        
                        <div class="mb-4">
                            <label class="form-label fw-bold">旅行タイトル</label>
                            <input type="text" name="title" class="form-control form-control-lg" value="<%= trip.title %>" required>
                        </div>

                        <div class="row g-4 mb-4">
                            
                            <div class="col-lg-6 d-flex flex-column">
                                <div class="bg-white border p-4 rounded shadow-sm mb-4 flex-fill">
                                    <h3 class="h6 fw-bold mb-3 border-bottom pb-2">
                                        <i class="bi bi-calendar-event me-2 text-primary"></i>日付選択
                                    </h3>
                                    <div class="row">
                                        <div class="col-md-6 mb-3 mb-md-0">
                                            <label class="form-label small fw-bold">開始日</label>
                                            <input type="date" name="start_date" class="form-control" value="<%= trip.start_date %>" required>
                                        </div>
                                        <div class="col-md-6">
                                            <label class="form-label small fw-bold">終了日</label>
                                            <input type="date" name="end_date" class="form-control" value="<%= trip.end_date %>" required>
                                        </div>
                                    </div>
                                </div>
                        
                                <div class="bg-white border p-4 rounded shadow-sm flex-fill">
                                    <h3 class="h6 fw-bold mb-3 border-bottom pb-2">
                                        <i class="bi bi-image me-2 text-primary"></i>カバー画像
                                    </h3>
                                    <div class="text-center">
                                        <div class="preview-container mb-3">
                                            <img id="imagePreview" src="<%= trip.image_url || '/images/default.jpg' %>" class="img-fluid rounded shadow-sm">
                                        </div>
                                        <input type="file" name="image_file" id="imageInput" class="form-control form-control-sm" accept="image/*">
                                        <div class="form-text mt-2">写真ライブラリから選択してください</div>
                                    </div>
                                </div>
                            </div>
                        
                            <div class="col-lg-6 d-flex flex-column">
                                <div class="bg-white border p-4 rounded shadow-sm mb-4 flex-fill">
                                    <h3 class="h6 fw-bold mb-3 border-bottom pb-2">
                                        <i class="bi bi-people me-2 text-success"></i>参加者リスト
                                    </h3>
                                    <div id="memberList" class="d-flex flex-wrap gap-4">
                                        <div class="text-center position-relative" style="width: 75px;">
                                            <img src="https://ui-avatars.com/api/?name=<%= username %>&background=random" class="rounded-circle border mb-1 member-avatar">
                                            <div class="small fw-bold text-truncate"><%= username %></div>
                                            <span class="badge bg-secondary" style="font-size: 0.5rem;">作成者</span>
                                        </div>
                                
                                        <div class="text-center position-relative member-item" style="width: 75px;">
                                            <button type="button" class="btn btn-danger btn-sm rounded-circle position-absolute top-0 end-0 p-0 d-flex align-items-center justify-content-center shadow-sm" 
                                                    style="width: 22px; height: 22px; transform: translate(5px, -5px);" 
                                                    onclick="removeMember('メンバーID')">
                                                <i class="bi bi-x" style="font-size: 16px;"></i>
                                            </button>
                                            <img src="https://ui-avatars.com/api/?name=Guest&background=random" class="rounded-circle border mb-1 member-avatar">
                                            <div class="small fw-bold text-truncate">GuestUser</div>
                                        </div>
                                    </div>
                                </div>
                        
                                <div class="bg-white border p-4 rounded shadow-sm flex-fill">
                                    <h3 class="h6 fw-bold mb-3 border-bottom pb-2">
                                        <i class="bi bi-person-plus me-2 text-success"></i>追加招待
                                    </h3>
                                    <div class="p-3 bg-light rounded">
                                        <div class="input-group mb-3">
                                            <input type="text" id="memberSearchInput" class="form-control form-control-sm" placeholder="名前 / ID / メール">
                                            <button class="btn btn-primary btn-sm px-3" type="button" onclick="searchAndAddMember()">検索</button>
                                        </div>
                                        <button type="button" class="btn btn-line btn-sm w-100 fw-bold shadow-sm" onclick="inviteViaLine()">
                                            <i class="bi bi-line me-2"></i>LINEで招待リンクを送る
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="d-grid mt-5">
                            <button type="submit" class="btn btn-primary btn-lg fw-bold shadow">
                                <i class="bi bi-cloud-arrow-up me-2"></i>変更を保存する
                            </button>
                            <a href="/trip_detail/<%= trip.id %>" class="btn btn-link text-muted mt-2 text-decoration-none">キャンセルして戻る</a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script src="/js/trip-edit.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>