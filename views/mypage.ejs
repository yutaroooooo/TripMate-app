<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>マイページ - TripMate</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/mypage.css">
</head>
<body class="bg-light">
    <%- include('partials/header', { username: user.name }) %>

    <main class="container py-5">
        <div class="row g-4">
            
            <div class="col-lg-4">
                <div class="card shadow-sm border-0 h-100">
                    <div class="card-body p-4 text-center">
                        <div class="profile-img-wrapper mb-3">
                            <img src="<%= user.profile_image || 'https://ui-avatars.com/api/?name=' + user.name + '&background=random' %>" 
                                 alt="プロフィール画像" class="rounded-circle shadow-sm border border-4 border-white">
                        </div>
                        
                        <h2 class="h4 fw-bold mb-4"><%= user.name %></h2>

                        <div class="d-grid gap-2">
                            <a href="/user_profile_edit" class="btn btn-outline-primary fw-bold py-2">
                                プロフィール編集
                            </a>

                            <button class="btn btn-line fw-bold py-2 text-white" id="lineLinkBtn">
                                <i class="bi bi-line me-2"></i>LINE連携
                            </button>

                            <form action="/logout" method="POST" class="w-100 mt-2">
                                <button type="submit" class="btn btn-link text-danger text-decoration-none small fw-bold">
                                    <i class="bi bi-box-arrow-right me-1"></i>ログアウト
                                </button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-lg-8">
                <div class="card shadow-sm border-0 h-100">
                    <div class="card-body p-4">
                        <h3 class="h5 fw-bold mb-4 border-bottom pb-2">
                            <i class="bi bi-clock-history me-2 text-secondary"></i>旅行履歴
                        </h3>
                        
                        <div class="history-scroll-area">
                            <% if (trips && trips.length > 0) { %>
                                <div class="list-group list-group-flush">
                                    <% trips.forEach(trip => { %>
                                        <a href="/trips/<%= trip.id %>" class="list-group-item list-group-item-action py-3 px-0 border-bottom">
                                            <div class="d-flex justify-content-between align-items-center">
                                                <div>
                                                    <div class="fw-bold text-dark"><%= trip.title %></div>
                                                    <div class="small text-muted">
                                                        <%= trip.start_date %> 〜 <%= trip.end_date %>
                                                    </div>
                                                </div>
                                                <i class="bi bi-chevron-right text-muted"></i>
                                            </div>
                                        </a>
                                    <% }) %>
                                </div>
                            <% } else { %>
                                <div class="text-center py-5 text-muted">
                                    <i class="bi bi-geo-alt-fill d-block mb-2 opacity-25" style="font-size: 3rem;"></i>
                                    <p>まだ参加した旅行はありません</p>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </main>

    <script src="/js/mypage.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>