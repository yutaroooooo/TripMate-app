<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>新規スポット追加 - TripMate</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/spot-create.css">
</head>
<body class="bg-light">
    <%- include('partials/header', { username: username }) %>

    <div class="container py-4">

        <div class="row g-4">
            <div class="col-lg-7">
                <div class="card shadow-sm border-0 h-100">
                    <div class="card-body">
                        <div class="mb-3">
                            <div class="input-group">
                                <span class="input-group-text bg-white border-end-0"><i class="bi bi-search"></i></span>
                                <input type="text" id="mapSearchInput" class="form-control border-start-0" placeholder="スポットを検索（例：東京タワー）">
                            </div>
                        </div>
                        <div id="mapPreview" class="bg-secondary rounded text-white d-flex align-items-center justify-content-center" style="height: 400px;">
                            地図がここに読み込まれます
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-lg-5">
                <div class="card shadow-sm border-0 h-100">
                    <div class="card-body d-flex flex-column">
                        <form action="/trips/<%= trip.id %>/spots" method="POST" class="flex-grow-1">
                            <div class="mb-3 p-3 bg-light rounded">
                                <label class="form-label small fw-bold text-muted">スポット名</label>
                                <div id="spotNameOutput" class="fw-bold h5 mb-0">未選択</div>
                                <input type="hidden" name="name" id="spotNameInput">
                            </div>

                            <div class="mb-4 p-3 bg-light rounded">
                                <label class="form-label small fw-bold text-muted">住所</label>
                                <div id="addressOutput" class="small">スポットを検索してください</div>
                                <input type="hidden" name="address" id="addressInput">
                            </div>

                            <div class="mb-4">
                                <label class="form-label small fw-bold">Instagram URL</label>
                                <div class="input-group">
                                    <span class="input-group-text"><i class="bi bi-instagram"></i></span>
                                    <input type="url" name="instagram_url" class="form-control" placeholder="https://www.instagram.com/p/...">
                                </div>
                                <div class="form-text">関連する投稿があればURLを入力</div>
                            </div>

                            <div class="d-grid mt-auto">
                                <button type="submit" class="btn btn-primary btn-lg fw-bold shadow-sm">
                                    旅程に追加
                                </button>
                                <a href="/trips/<%= trip.id %>" class="btn btn-link text-muted mt-2 text-decoration-none small">キャンセル</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="/js/spot-create.js"></script>
</body>
</html>