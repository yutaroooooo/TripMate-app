<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm mb-4">
    <div class="container">
        <a class="navbar-brand fw-bold" href="/trips">TripMate</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto align-items-center">
                <li class="nav-item">
                    <span class="nav-link text-white me-2">ようこそ、<%= username %> さん</span>
                </li>
            
                <% if (typeof email !== 'undefined' && email === 'yutaro.onuki9816@gmail.com') { %>
                    <li class="nav-item">
                        <a class="nav-link text-warning fw-bold me-2" href="/admin/monitoring">
                            <i class="bi bi-shield-lock"></i> 管理画面
                        </a>
                    </li>
                <% } %>
            
                <li class="nav-item mb-2 mb-lg-0">
                    <a class="btn btn-outline-light btn-sm px-3" href="/mypage">マイページ</a>
                </li>
                <li class="nav-item">
                    <form action="/logout" method="POST" class="d-inline ms-lg-2">
                        <button type="submit" class="btn btn-outline-light btn-sm px-3">ログアウト</button>
                    </form>
                </li>
            </ul>
        </div>
    </div>
</nav>