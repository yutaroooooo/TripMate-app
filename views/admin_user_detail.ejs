<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ユーザー詳細 - TripMate 管理者</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
</head>
<body class="bg-light">
    <%- include('partials/header', { username: user.name }) %>

    <main class="container py-4">
        <nav aria-label="breadcrumb" class="mb-4">
            <ol class="breadcrumb small">
                <li class="breadcrumb-item"><a href="/admin/users">ユーザー一覧</a></li>
                <li class="breadcrumb-item active">ユーザー詳細</li>
            </ol>
        </nav>

        <div class="card shadow-sm border-0 mb-4 p-3">
            <div class="card-body d-flex align-items-center">
                <% if (targetUser.profile_image) { %>
                    <img src="<%= targetUser.profile_image %>" 
                         class="rounded-circle shadow-sm border me-4" 
                         style="width: 60px; height: 60px; object-fit: cover;">
                <% } else { %>
                    <div class="bg-secondary text-white rounded-circle d-flex align-items-center justify-content-center me-4" 
                         style="width: 60px; height: 60px;">
                        <i class="bi bi-person-fill h3 mb-0"></i>
                    </div>
                <% } %>
                <div>
                    <h2 class="h5 fw-bold mb-1"><%= targetUser.name %></h2>
                    <p class="text-muted small mb-0">Email: <%= targetUser.email %> | User ID: <%= targetUser.id %></p>
                </div>
                <div class="ms-auto">
                    <span class="badge <%= targetUser.isActive ? 'bg-success' : 'bg-danger' %> px-3 py-2">
                        <%= targetUser.isActive ? '有効なユーザー' : '停止中のユーザー' %>
                    </span>
                </div>
            </div>
        </div>

        <div class="row g-4">
            <div class="col-md-6">
                <div class="card shadow-sm border-0 h-100">
                    <div class="card-header bg-white fw-bold">統計・履歴</div>
                    <div class="card-body">
                        <table class="table table-sm">
                            <tr>
                                <td>作成した旅行数</td>
                                <td class="text-end fw-bold"><%= stats.tripCount %> 件</td>
                            </tr>
                            <tr>
                                <td>累計スポット数</td>
                                <td class="text-end fw-bold"><%= stats.spotCount %> 件</td>
                            </tr>
                            <tr>
                                <td>被通報回数</td>
                                <td class="text-end fw-bold text-danger"><%= stats.reportCount %> 回</td>
                            </tr>
                        </table>
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card shadow-sm border-0 h-100 border-top border-dark border-5">
                    <div class="card-header bg-white fw-bold">アカウント操作</div>
                    <div class="card-body d-grid gap-2">
                        <button class="btn btn-outline-dark" onclick="alert('パスワードリセットメールを送信しました（モック）')">
                            パスワードリセット送信
                        </button>
                        
                        <form action="/admin/users/<%= targetUser.id %>/toggle-status-detail" method="POST">
                            <button type="submit" class="btn <%= targetUser.isActive ? 'btn-danger' : 'btn-success' %> fw-bold w-100">
                                <%= targetUser.isActive ? 'このアカウントを凍結する' : '凍結を解除する' %>
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </main>
</body>
</html>